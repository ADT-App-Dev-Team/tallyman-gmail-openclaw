{
  "id": "@tallyman/openclaw-gmail",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Tallyman API key (tk_*). Falls back to TALLYMAN_API_KEY env var."
      },
      "baseUrl": {
        "type": "string",
        "description": "Tallyman API base URL. Falls back to TALLYMAN_BASE_URL env var."
      }
    }
  }
}
